<%= form_for ProjectComment.new , remote: true do |f| %>
<div class="form-group">

  <h4>Content</h4>

  <%= f.text_area :content, class: "form-control" %>
  <%= hidden_field_tag :project_id ,@project.id %>
</div>

    <div class="form-group">
      <%= f.submit "Comment", class: "btn btn-primary" %>
    </div>

<% end %>
