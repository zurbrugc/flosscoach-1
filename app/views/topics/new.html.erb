<div class="page-content">
  <div class="panel panel-bordered">
    <div class="panel-heading">
      <h3 class="panel-title">New Topic on <%= @project.name %> forum</h3>
        </div>
          <div class="panel-body">
            <%= render 'form', topic: @topic %>
          </div>
        <div class="panel-footer"> 
      <%= link_to "Back to #{@project.name} forum", forum_path(@forum.project_id), class:"btn btn-danger" %>
    </div>
  </div>
</div>
